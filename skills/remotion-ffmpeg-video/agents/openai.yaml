interface:
  display_name: "Remotion + FFmpeg Video"
  short_description: "Build and verify programmatic video"
  default_prompt: "Use $remotion-ffmpeg-video to build and verify this video."
policy:
  allow_implicit_invocation: true
